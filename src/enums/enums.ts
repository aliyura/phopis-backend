export enum UserRole {
  ADMIN = 'ADMIN',
  STAFF = 'STAFF',
  BUSINESS = 'BUSINESS',
}
export enum Status {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  SUSPENDED = 'SUSPENDED',
}

export enum BusinessTarget {
  MANY = 'MANY',
  PRODUCT = 'PRODUCT',
  SERVICE = 'SERVICE',
}

export enum AccountType {
  BUSINESS = 'BUSINESS',
  INDIVIDUAL = 'INDIVIDUAL',
}
