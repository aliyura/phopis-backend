export enum UserRole {
  ADMIN = 'ADMIN',
  STAFF = 'STAFF',
  BUSINESS = 'BUSINESS',
}
export enum UserStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  SUSPENDED = 'SUSPENDED',
}

export enum BusinessTarget {
  MANY = 'MANY',
  PRODUCT = 'PRODUCT',
  SERVICE = 'SERVICE',
}
